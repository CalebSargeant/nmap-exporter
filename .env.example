# Choose target source: file | azure | aws
TARGET_SOURCE=file

# Prometheus exporter port
EXPORTER_PORT=9808

# Scan frequency in seconds
SCAN_FREQUENCY=3600

# Optional Nmap settings
# Comma-separated list or range, e.g. 22,80,443 or 1-1024
# NMAP_PORTS=22,80,443
# Additional arguments passed to nmap (keep it safe)
NMAP_ARGUMENTS=-Pn -T4

# File source
# Path inside container or local path when running without Docker
TARGET_FILE=./portscanip.nmap

# Azure source (array of credential objects)
# AZURE_CREDENTIALS='[{"AZURE_CLIENT_ID":"...","AZURE_CLIENT_SECRET":"...","AZURE_TENANT_ID":"..."}]'

# AWS source (array of credential objects)
# AWS_CREDENTIALS='[{"AWS_ACCESS_KEY_ID":"...","AWS_SECRET_ACCESS_KEY":"...","AWS_PROFILE_NAME":"default","AWS_REGIONS":["eu-west-1"]}]'
